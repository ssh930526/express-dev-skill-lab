<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Welcome to Express Dev Skills | Here are your Skills</title>
</head>
<body>
    <h1>Here are your Skills as of <%= time %></h1>
    <a href="/">Home</a>
    <ul>
        <% skills.forEach(function(skill, idx) { %>
            <li>
                <form action="/skills/<%= idx %>?_method=DELETE" method="POST">
                    <button type="submit">❌</button>
                </form>
                <a href="/skills/<%= idx %>">
                <%= skill.text %>
                -
                <%= skill.done ? 'done' : 'not done' %>
            </a>
            </li>

        <% }) %>
    </ul>

    <a href="/skills/new">Add Dev-Skill</a>

</body>
</html>